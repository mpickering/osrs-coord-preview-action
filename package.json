{
  "name": "osrs-coordinate-preview-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action client for osrs-coordinate-preview.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build:ts && npm run build:bundle",
    "build:ts": "tsc -p tsconfig.json",
    "build:bundle": "ncc build dist/src/main.js -o dist",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vercel/ncc": "^0.38.4",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
